# Public Demo Note

This public copy intentionally excludes private generated model images and source clothing assets.
The private deploy repo contains `static/generated/flux2/*.png` for runtime image selection.
